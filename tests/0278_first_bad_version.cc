#include "0278_first_bad_version.h"

#include <gtest/gtest.h>

using namespace leetcode::first_bad_version;

TEST(FIRST_BAD_VERSION, EXAMPLE_TEST) {
    ASSERT_EQ(Solution::first_bad_version(5), 4);
}
